# scripts/generate_logo.py
from PIL import Image, ImageDraw

img = Image.new('RGB', (512,512), color=(30,144,255))
d = ImageDraw.Draw(img)
d.text((120, 220), "Apex Digital AI", fill=(255,255,255))
img.save("client_project_flow/project_001/design/logo.png")
